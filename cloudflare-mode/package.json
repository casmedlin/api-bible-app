{
  "name": "api-bible-app-cloudflare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --port 3002",
    "deploy": "wrangler pages deploy",
    "preview": "wrangler pages dev"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
